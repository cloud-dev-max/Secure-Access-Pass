# V7.1 Polish & UI Navigation Fixes - Implementation Plan

## Issue 1: Scanner Generic Denial Messages ✅
**File**: `app/scanner/page.tsx`
**Problem**: Line 192 duplicates the message construction when API already returns formatted `user_name`
**Fix**: Use `data.user_name` directly from API response instead of reconstructing

## Issue 2: Scanner Duplicated Guest Count ✅  
**File**: `app/scanner/page.tsx`
**Problem**: Line 192 constructs message, but API already formats it at line 376
**Fix**: Remove duplicate formatting, use API response directly

## Issue 3: Occupancy Math Bug ✅
**File**: `app/api/occupancy/route.ts` needs to be checked
**Problem**: Current Occupancy only counts residents, not guests
**Fix**: Add `SUM(active_guests)` to the occupancy calculation

## Issue 4: Dashboard Clickable Cards ✅
**File**: `app/dashboard/page.tsx`
**Location**: Lines ~800-880 (overview cards)
**Fix**: Wrap cards in `<button>` with `onClick={() => setActiveTab('...')}`

## Issue 5: Current Occupancy Tab ✅
**Files**: `app/dashboard/page.tsx`
**Changes Needed**:
1. Add 'occupancy' to activeTab type (line 57)
2. Add new tab button in navigation
3. Create new tab content with full table
4. Remove modal (`showInsideModal`)
5. Change "Current Occupancy" card click to switch tabs

## Issue 6: Revenue Loading Bug ✅
**File**: `app/dashboard/page.tsx`
**Problem**: Line 105 calls `loadRevenueData()` before useEffect, but function is defined later
**Fix**: Move `loadRevenueData()` call inside useEffect after mount

## Issue 7: Activity Log Colors ✅
**File**: `app/dashboard/page.tsx`
**Location**: Lines ~1010-1050 (Recent Activity section)
**Fix**: Apply different background colors based on `log.scan_type` and `log.result`:
- ENTRY + GRANTED = green
- EXIT + GRANTED = blue  
- DENIED = red
- FORCE_EXIT = red

## Implementation Order

1. Fix Scanner (Issues 1 & 2) - Single file
2. Fix Occupancy Math (Issue 3) - API file  
3. Fix Dashboard Overview (Issues 4, 6, 7) - Dashboard changes
4. Add Occupancy Tab (Issue 5) - Major dashboard addition
